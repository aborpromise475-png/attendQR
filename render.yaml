services:
  - type: web
    name: attendiq
    runtime: node
    plan: free
    rootDir: AttendIQ/backend
    buildCommand: npm install
    startCommand: npm start
    autoDeployTrigger: commit
    envVars:
      - key: ATTENDIQ_SECRET
        generateValue: true